server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # React Router 使用 BrowserRouter；不存在的實體路徑必須交回 SPA 處理。
    location / {
        try_files $uri $uri/ /index.html;
    }
}
